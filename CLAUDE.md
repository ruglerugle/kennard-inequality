# このリポジトリについて

不確定性原理 Δx·Δp ≥ ℏ/2 のケナードによる証明（1927年）を、全7ステージでたどる学習クエストサイト。
公開URL: https://ruglerugle.github.io/kennard-inequality/

**このサイトの主役は「公式がどうやって発見されたのか」**。天下りの式を並べるのではなく、
発見の論理（借りてきた道具・類推・ゴールからの逆算）を読者が追体験できることを最優先する。

# コミュニケーション
- 日本語で応答する（コード・変数名は英語）
- 簡潔に回答し、自明な説明は省略する
- 複雑なタスクでは実装前に計画を提示し、承認後に着手する

# Git規約
- Conventional Commits形式、本文は日本語（例: `feat: ユーザー認証にOAuth2を追加`）
- 確認なしに自動コミット・自動pushしない
- pushするとGitHub Pagesに即反映される。ビルドが走らないことがあるので、
  反映されない場合は `gh api -X POST repos/ruglerugle/kennard-inequality/pages/builds` で手動トリガー

# 対象読者（最重要）

**大学レベルの内容を、優秀な中学生にもわかるように解説する**のが目標。説明不足が最大のNG。

- 専門用語（振動数・運動量・標準偏差・積分・複素数など）は初出時に必ず中学生向けの言葉で定義する
- 「AだからB」の各ステップを飛ばさない。1つの結論に複数ターンの問答をかけてよい
- 式は導出の途中経過まで書き、記号の読み下しを添える（∫ は「インテグラル」と読む、など）
- 日常のアナロジーを積極的に使う
- 「高校で習った」のような、中学生を除外する前提の言い回しを避ける
- **分量が増えることをためらわない。簡潔さより丁寧さを優先する**

# キャラクターのセリフ

**うさ美 ＝「優秀な中学生」役**
- **知識は中学生レベルまで**。高校以上の事実（運動量、正弦波、相対性理論、うなり等）を
  「〜でしたね」と既知のように語らせない。それらは必ずねこ博士が教える
- **思考力はずば抜けている**。教わった直後の材料から帰結を導く・矛盾を突く・鋭い質問をする・
  アナロジーを自作する、が彼女の見せ場。「ということは……」で先回りして正しく推論するのは大いに可
- 卑屈・無知アピールはキャラ崩壊。分からないことは堂々と質問する
- 例外: 複素数（complex-quest）と量子力学の基礎（quantum-quest）は学習済みという設定。
  ただし復習として自分で整理し直す形にし、それらを見ていない読者にも完結した説明にする

**ねこ博士**
- 柔らかい先生口調（「〜ね」「〜なんだ」「〜だよ」「〜かな？」「〜してごらん」）
- 相手に同意を迫る「〜だろう？」「〜じゃないか？」は使わない（自問の「〜だろう？」は可）
- 用語・事実・歴史はすべて博士が供給する

**共通**
- 授業型なので「次のステージで」等のメタ言及は許容（物語型ゲームでは禁止）
- セリフを書いたら (1)「だろう？」「じゃないか？」をgrep、
  (2) うさ美の全セリフについて「この知識は中学生が持っているか？ 直前に博士が教えたことからの推論か？」を確認
- **回りくどい前置きを入れない**。疑問は端的にぶつけさせる
- 会話の中で引用（カギ括弧）を使うときは、参照先が直前にあることを確認する

# 表記ルール
- 累乗は `^` ではなく上付き（`e<sup>ikx</sup>`）で書く
- 同じ式を変形して別の向きで書くときは、同じ式である旨の注釈を入れる（例: λ＝h/p に「※ p＝h/λ と同じ式」）

# ファイル構成
- `design-system.css` — quest-template由来の共通デザイン。配色は `:root` の変数で管理（夜の藍×ゴールド）
- `js/progress.js` — 進捗管理・クイズ判定。**クイズの正解は `ANSWERS`**。localStorageキーは `kennardQuestProgress`、グローバルは `KQ`
- `stage1.html` 〜 `stage7.html` — 各ステージ。`index.html`（表紙）、`complete.html`（クリア画面）
- `images/` — キャラ画像はWebP（幅300px程度）。`<img>` には `width`/`height` 属性を必ず付ける。
  `labo.webp`（stage2の座標軸図の背景）の元になった高解像度PNGは `.gitignore` で除外してあり、
  ローカルPCにしかない
- 図はSVGで手書きし、JSでアニメーションさせる場合は `prefers-reduced-motion` で静止画にフォールバックする

# 禁止事項
- README・ドキュメントを勝手に生成・変更しない
- テストコードを確認なしに削除・コメントアウトしない
- 既存の動作するコードを理由なくリファクタリングしない
